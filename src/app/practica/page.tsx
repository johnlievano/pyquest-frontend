import { PracticeWorkspace } from "@/components/practice/practice-workspace";

// Esta página renderiza todo el workspace de práctica.
export default function PracticaPage() {
  return <PracticeWorkspace />;
}
